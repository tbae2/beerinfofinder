var React = require('react');
var PropTypes = React.PropTypes;

var main = React.createClass({

  render: function() {
    return (
      <div>
        Hello world!
      </div>
    );
  }

});

module.exports = main;
